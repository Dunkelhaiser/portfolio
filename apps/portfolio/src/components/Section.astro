---
interface Props {
    title: string;
    id?: string;
}

const { title, id } = Astro.props;
---

<section id={id} class="flex flex-col items-center scroll-mt-12">
    <h2 class="text-3xl font-bold md:text-4xl mb-4">{title}</h2>
    <slot />
</section>