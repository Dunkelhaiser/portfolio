export interface Skill {
    name: string;
    icon: string;
    link?: string;
}

export interface TimelineItem {
    date?: string;
    title: string;
    subtitle?: string;
    logo?: string;
    description?: string;
    link?: string;
}
