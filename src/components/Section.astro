---
interface Props {
    title: string;
}

const { title } = Astro.props;
---

<section class="flex flex-col items-center">
    <h2 class="text-3xl font-bold md:text-4xl mb-4">{title}</h2>
    <slot />
</section>